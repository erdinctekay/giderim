# Port the server listens on
PORT=4000

# Comma-separated list of allowed origin domains for CORS.
# Each entry matches the exact hostname and all subdomains.
# Example: "example.com" allows "example.com" and "*.example.com".
CORS_ALLOWED_DOMAINS=example.com
