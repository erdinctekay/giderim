# Public app URL (required for build; used in index.html for og:url).
VITE_APP_URL=https://yourdomain.com

# Evolu sync server URL.
# When set, the app syncs data with this server.
# When empty/unset, the app runs in local-only mode (no sync).
VITE_SYNC_URL=http://localhost:4000
